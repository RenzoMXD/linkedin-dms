[project]
name = "desearch-dms"
version = "0.0.1"
description = "Community-built DM sync service (X/Twitter first)"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "pydantic>=2.6",
]

[tool.uvicorn]
factory = false
